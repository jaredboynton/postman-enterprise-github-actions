# Organization-level secrets (set at GitHub org level, not in .env):
# ORG_PROPAGATOR_TOKEN=github_pat_with_repo_and_workflow_scope
# ORG_DISPATCH_TOKEN=github_pat_with_repo_scope_for_automated_variable_storage

# Repository-level secrets (set in each repository):
# Postman API Configuration
POSTMAN_API_KEY=postman_service_account_api_key_here

# User access control (set to 'true' to bypass team membership validation)
POSTMAN_ALLOW_ALL_USERS=false